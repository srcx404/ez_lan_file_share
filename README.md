# ez_lan_file_share

- A simple http file share server
When running locally, it can share files between different devices via http protocal;
When running on the Internet, it can share files globally;

The program will create folder "shared_files" on the first run, then you can put sharing files here. 

It runs on port 5000
